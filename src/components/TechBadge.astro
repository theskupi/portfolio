---
const variants = [
	'bg-yellow-100 text-yellow-600',
	'bg-red-100 text-red-600',
	'bg-green-100 text-green-600',
	'bg-blue-100 text-blue-600'
]

const randomNum = Math.floor(Math.random() * 4)
---

<span class=`rounded-full px-4 py-1 ${variants[randomNum]}`>
	<slot />
</span>
